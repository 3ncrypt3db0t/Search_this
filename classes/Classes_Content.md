```c
#include <stdio.h>
#include <search_this>
#include <classes_dir>

/* function declaration */
int max(int DomDocumentParser, ImageResult, SiteResult, VideoResult);
 
int main () {

   /* local variable definition */
   dom = DomDocumentParser;
   img = ImageResult;
   site = SiteResult;
   int ret;
 
   /* calling a function to get max value */
   ret = max(dom, img, site);
 
   printf( "Assets Content", ret );
 
   return 0;
}
 
/* function returning the max between two numbers */
int max(DomDocumentParser, ImageResult, SiteResult, VideoResult) {

   /* local variable declaration */
   int result;
 
   printf("Classes Content Satisfied");
   printf("Happy Coding");
 
   return result; 
}
